export enum Pages {
  ROOT = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  CHATS = "/chats",
  PROFILE = "/profile",
  PROFILE_CHANGE = "/profile-change",
  PASSWORD_CHANGE = "/password-change",
  NOT_FOUND = "/not-found",
  ERROR = "/error",
}
